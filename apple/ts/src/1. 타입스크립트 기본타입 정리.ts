let myName = 'yang'
let age = 26
let local = 'paju'


let singer: { name: string, song: string } = { name: 'iu', song: '좋은날' }


let project: { member: string[], days: number, started: boolean } = {
  member: ['kim', 'park'],
  days: 30,
  started: true,
}